# 팀 색상 설정 가이드 (Team Color Configuration)

## 🔴 문제: 완전히 다른 색인데 같은 팀으로 인식됨

**원인**: `adaptive_clustering` 방식이 HSV 범위를 무시하고 자동으로 색상을 그룹화했습니다.

**해결**: `fixed_range` 방식으로 변경 + 정확한 HSV 범위 설정

---

## ✅ 이미 적용된 수정사항

### 1. 팀 분류 방식 변경
[tracker_config.py](tracker_config.py:68):
```python
TEAM_DETECTION_METHOD = 'fixed_range'  # 고정 HSV 범위 사용 ✅
```

### 2. 디버그 모드 활성화
[tracker_config.py](tracker_config.py:89):
```python
DEBUG_TEAM_COLORS = True  # HSV 값 출력 ✅
```

---

## 🎨 HSV 색상 범위 설정하기

### 현재 설정 (기본값)
[tracker_config.py](tracker_config.py:57-59):
```python
TEAM_A_HSV_RANGE = ((90, 50, 50), (130, 255, 255))    # 파란색
TEAM_B_HSV_RANGE = ((0, 0, 180), (180, 30, 255))      # 흰색
REFEREE_HSV_RANGE = ((0, 0, 0), (180, 255, 60))       # 검은색
```

**⚠️ 중요: 이 값들을 당신의 실제 팀 유니폼 색상에 맞게 수정해야 합니다!**

---

## 📊 HSV 색상 이해하기

HSV는 3개 채널로 구성:
- **H (Hue)**: 색상 (0-180)
  - 0-10: 빨강
  - 20-40: 주황/노랑
  - 40-80: 녹색
  - 90-130: 파랑
  - 140-170: 보라/핑크

- **S (Saturation)**: 채도 (0-255)
  - 0-50: 무채색 (회색, 흰색, 검정)
  - 50-150: 약간 채도
  - 150-255: 높은 채도 (선명한 색)

- **V (Value)**: 밝기 (0-255)
  - 0-60: 어두움 (검정)
  - 60-180: 중간
  - 180-255: 밝음 (흰색)

---

## 🔍 방법 1: 디버그 모드로 실제 색상 확인하기 (추천!)

### 단계:
1. **현재 설정으로 실행**:
```bash
python tracker.py
```

2. **콘솔 출력 확인**:
```
🎨 Detected HSV: H=105.3, S=142.7, V=185.2
  → Team A matched!

🎨 Detected HSV: H=15.8, S=25.4, V=235.1
  → Team B matched!

🎨 Detected HSV: H=0.0, S=5.2, V=35.8
  → Referee matched!

🎨 Detected HSV: H=110.5, S=88.3, V=195.4
  → Unknown (no match)  ← 이런 게 보이면 범위 조정 필요!
```

3. **"Unknown" 색상 값을 보고 범위 조정**:
   - Unknown으로 나오는 선수가 실제로는 Team A라면
   - 해당 HSV 값을 포함하도록 TEAM_A_HSV_RANGE 확장

---

## 🎯 방법 2: 수동으로 HSV 범위 찾기

### Python 스크립트로 색상 확인:

```python
import cv2
import numpy as np

# 1. 비디오에서 한 프레임 추출
cap = cv2.VideoCapture("zoomed_game.mp4")
cap.set(cv2.CAP_PROP_POS_FRAMES, 100)  # 100번째 프레임
ret, frame = cap.read()

# 2. 선수의 유니폼 부분을 직접 선택 (좌표 조정 필요)
jersey_region = frame[100:200, 300:400]  # y1:y2, x1:x2

# 3. HSV로 변환
hsv = cv2.cvtColor(jersey_region, cv2.COLOR_BGR2HSV)

# 4. 평균 HSV 값 계산
avg_hsv = np.mean(hsv, axis=(0, 1))
print(f"평균 HSV: H={avg_hsv[0]:.1f}, S={avg_hsv[1]:.1f}, V={avg_hsv[2]:.1f}")

# 5. HSV 범위 추천
h, s, v = avg_hsv
print(f"\n추천 범위:")
print(f"((  {max(0, h-15):.0f}, {max(0, s-50):.0f}, {max(0, v-50):.0f}), "
      f"({min(180, h+15):.0f}, 255, 255))")
```

---

## 📝 일반적인 유니폼 색상 HSV 범위

### 빨간색 유니폼:
```python
TEAM_A_HSV_RANGE = ((0, 100, 100), (10, 255, 255))     # 빨강 1
TEAM_A_HSV_RANGE = ((170, 100, 100), (180, 255, 255))  # 빨강 2 (HSV에서 빨강은 0과 180 양쪽)
```

### 파란색 유니폼:
```python
TEAM_A_HSV_RANGE = ((90, 50, 50), (130, 255, 255))  # 파랑
```

### 노란색 유니폼:
```python
TEAM_A_HSV_RANGE = ((20, 100, 100), (40, 255, 255))  # 노랑
```

### 녹색 유니폼:
```python
TEAM_A_HSV_RANGE = ((40, 50, 50), (80, 255, 255))  # 녹색
```

### 흰색 유니폼:
```python
TEAM_B_HSV_RANGE = ((0, 0, 180), (180, 30, 255))  # 흰색 (낮은 채도 + 높은 밝기)
```

### 검은색 유니폼:
```python
REFEREE_HSV_RANGE = ((0, 0, 0), (180, 255, 60))  # 검정 (낮은 밝기)
```

### 회색 유니폼:
```python
TEAM_HSV_RANGE = ((0, 0, 80), (180, 50, 200))  # 회색 (낮은 채도 + 중간 밝기)
```

---

## 🛠️ 실제 설정 예시

### 예시 1: 파란색 vs 흰색
```python
TEAM_A_HSV_RANGE = ((90, 80, 80), (130, 255, 255))    # 파란색 (채도 높음)
TEAM_B_HSV_RANGE = ((0, 0, 180), (180, 30, 255))      # 흰색 (채도 낮음)
REFEREE_HSV_RANGE = ((0, 0, 0), (180, 255, 60))       # 검정
```

### 예시 2: 빨간색 vs 파란색
```python
TEAM_A_HSV_RANGE = ((0, 100, 100), (10, 255, 255))    # 빨강
TEAM_B_HSV_RANGE = ((90, 100, 100), (130, 255, 255))  # 파랑
REFEREE_HSV_RANGE = ((0, 0, 180), (180, 30, 255))     # 흰색 (심판)
```

### 예시 3: 노란색 vs 녹색
```python
TEAM_A_HSV_RANGE = ((20, 100, 100), (40, 255, 255))   # 노랑
TEAM_B_HSV_RANGE = ((40, 80, 80), (80, 255, 255))     # 녹색
REFEREE_HSV_RANGE = ((0, 0, 0), (180, 255, 60))       # 검정
```

---

## 🚨 범위 조정 팁

### 범위가 너무 좁으면:
- 증상: 많은 "Unknown" 선수가 나옴
- 해결: 범위를 넓혀라
```python
# Before: 너무 좁음
TEAM_A_HSV_RANGE = ((100, 150, 150), (110, 200, 200))

# After: 적절하게 넓힘
TEAM_A_HSV_RANGE = ((90, 80, 80), (130, 255, 255))
```

### 범위가 너무 넓으면:
- 증상: 다른 팀이 같은 팀으로 인식됨
- 해결: 범위를 좁혀라
```python
# Before: 너무 넓음 (모든 색 포함)
TEAM_A_HSV_RANGE = ((0, 0, 0), (180, 255, 255))

# After: 특정 색상만
TEAM_A_HSV_RANGE = ((90, 80, 80), (130, 255, 255))
```

### 채도(S)와 밝기(V) 조정:
- **채도 범위 넓히기**: 색이 흐릿한 경우
```python
# S 최소값을 낮춤: 50 → 30
TEAM_A_HSV_RANGE = ((90, 30, 80), (130, 255, 255))
```

- **밝기 범위 넓히기**: 그림자/조명 변화가 심한 경우
```python
# V 최소값을 낮춤: 80 → 50
TEAM_A_HSV_RANGE = ((90, 80, 50), (130, 255, 255))
```

---

## 📋 체크리스트

### 1단계: 디버그 모드 활성화
- [x] `TEAM_DETECTION_METHOD = 'fixed_range'` ✅
- [x] `DEBUG_TEAM_COLORS = True` ✅

### 2단계: 비디오 실행 & 콘솔 확인
```bash
python tracker.py
```

### 3단계: HSV 값 관찰
- Unknown 선수들의 HSV 값을 메모하세요
- 같은 팀인데 다른 색으로 인식되는 경우 HSV 값 확인

### 4단계: 범위 조정
- [tracker_config.py](tracker_config.py)에서 `TEAM_A_HSV_RANGE`, `TEAM_B_HSV_RANGE` 수정
- 디버그 출력을 보고 범위를 넓히거나 좁히기

### 5단계: 재실행 & 확인
```bash
python tracker.py
```

### 6단계: 디버그 모드 끄기 (완료 후)
```python
DEBUG_TEAM_COLORS = False  # 출력 줄이기
```

---

## 🎯 당신의 팀 색상은?

**지금 당장 확인하는 방법:**

1. 비디오 실행:
```bash
python tracker.py
```

2. 콘솔에서 출력되는 HSV 값 확인:
```
🎨 Detected HSV: H=???, S=???, V=???
```

3. 이 값들을 보고 범위 조정!

---

## 💬 도움이 필요하면:

**당신의 팀 색상을 알려주세요:**
- 팀 A: 무슨 색? (예: "밝은 파란색", "진한 빨간색")
- 팀 B: 무슨 색? (예: "흰색", "노란색")
- 심판: 무슨 색? (예: "검정", "줄무늬")

**또는 디버그 모드 실행 후:**
- "Unknown" 선수들의 HSV 값을 알려주세요
- 제가 정확한 범위를 계산해드립니다!

---

## 📝 요약

**이미 수정된 것:**
1. ✅ 팀 분류 방식: `adaptive_clustering` → `fixed_range`
2. ✅ 디버그 모드: `DEBUG_TEAM_COLORS = True`
3. ✅ 잔상 감소: `DOT_FADE_ALPHA = 0.70`

**해야 할 것:**
1. ⚠️ **HSV 범위 조정** - 당신의 팀 색상에 맞게!
2. ⚠️ 비디오 실행하고 콘솔 출력 확인
3. ⚠️ Unknown 값들을 보고 ���위 수정

---

**지금 바로 테스트하세요:**
```bash
python tracker.py
```

콘솔에 나오는 HSV 값을 보고 알려주시면 정확한 범위를 계산해드립니다! 🎨
