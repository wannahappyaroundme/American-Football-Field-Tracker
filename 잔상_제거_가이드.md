# 전술 맵 잔상 제거 가이드

## 문제: 빠른 움직임에서 잔상이 너무 많이 남음 👻

스포츠 영상에서 선수들이 빠르게 움직이면 전술 맵(오른쪽)에 점들이 계속 남아서 지저분해 보입니다.

---

## ✅ 해결 완료! (v2.2.1)

`DOT_FADE_ALPHA`를 **0.98 → 0.85**로 변경하여 잔상이 훨씬 빠르게 사라집니다!

---

## 🎯 추천 설정 (이미 적용됨)

[tracker_config.py](tracker_config.py):
```python
PERSISTENT_DOTS = True      # 잔상 유지 활성화
DOT_FADE_ALPHA = 0.85       # 빠른 페이드 (잔상 감소) ← 추천!
```

**효과:**
- ✅ 최근 움직임은 보이지만 오래된 잔상은 빠르게 사라짐
- ✅ 깨끗한 전술 맵
- ✅ 선수 이동 경로는 여전히 추적 가능

---

## 🔧 추가 조정 옵션

### 옵션 1: 더 깨끗하게 (잔상 거의 없음)
```python
DOT_FADE_ALPHA = 0.70       # 매우 빠른 페이드
```

### 옵션 2: 잔상 완전 제거 (가장 깨끗함)
```python
PERSISTENT_DOTS = False     # 잔상 완전 제거
```
**효과:** 매 프레임 새로 그려짐, 잔상 0%

### 옵션 3: 원래 설정으로 되돌리기 (많은 잔상)
```python
DOT_FADE_ALPHA = 0.98       # 느린 페이드 (원래 설정)
```

---

## 📊 설정 비교

| 설정 값 | 잔상 정도 | 추천 상황 |
|--------|---------|---------|
| `PERSISTENT_DOTS = False` | 0% (완전 제거) | 가장 깨끗한 화면 원할 때 |
| `DOT_FADE_ALPHA = 0.70` | 5-10% | 거의 잔상 없이 |
| `DOT_FADE_ALPHA = 0.85` | 15-20% | **균형잡힌 설정** ⭐ |
| `DOT_FADE_ALPHA = 0.95` | 40-50% | 약간의 경로 추적 |
| `DOT_FADE_ALPHA = 0.98` | 80-90% | 전체 경로 추적 (원래) |
| `DOT_FADE_ALPHA = 1.0` | 100% (영구) | 절대 안 사라짐 |

---

## 🎬 시각적 효과

### Before (0.98 - 많은 잔상):
```
전술 맵: ●●●●●●●●●●●●●●●
        (선수가 지나간 모든 경로가 남음)
```

### After (0.85 - 빠른 페이드): ⭐ 현재 설정
```
전술 맵: ●●●○○○○···········
        (최근 위치만 선명, 오래된 건 빠르게 사라짐)
```

### 완전 제거 (False):
```
전술 맵: ●
        (현재 위치만, 잔상 없음)
```

---

## 🚀 바로 테스트하기

```bash
python tracker.py
```

**기본 설정 (0.85)**로 실행됩니다. 오른쪽 전술 맵을 보시면 잔상이 훨씬 빠르게 사라지는 것을 확인할 수 있습니다!

---

## 🎯 원하는 효과에 맞게 조정

### 상황 1: "잔상이 여전히 많아요"
→ [tracker_config.py](tracker_config.py)에서:
```python
DOT_FADE_ALPHA = 0.70  # 더 빠르게
```

### 상황 2: "완전히 깨끗하게 하고 싶어요"
→ [tracker_config.py](tracker_config.py)에서:
```python
PERSISTENT_DOTS = False  # 잔상 완전 제거
```

### 상황 3: "약간의 경로는 보고 싶어요"
→ 현재 설정(0.85)이 완벽합니다! ✅

---

## 💡 팁

**빠른 스포츠 영상 (축구, 농구, 미식축구):**
- 추천: `DOT_FADE_ALPHA = 0.70 ~ 0.85`
- 선수들이 빠르게 움직이므로 빠른 페이드가 좋음

**느린 전술 분석:**
- 추천: `DOT_FADE_ALPHA = 0.95 ~ 0.98`
- 전체 이동 경로를 보고 싶을 때

**프레젠테이션/보고서용 (깨끗함 우선):**
- 추천: `PERSISTENT_DOTS = False`
- 잔상 없이 현재 위치만 표시

---

## 📝 요약

**변경사항:**
- ✅ 페이드 속도: 0.98 → **0.85** (훨씬 빠르게 사라짐)
- ✅ 잔상 감소: ~80% → **~15-20%**
- ✅ 더 깨끗한 전술 맵
- ✅ 여전히 최근 이동 경로 추적 가능

**테스트 방법:**
```bash
python tracker.py
```

**추가 조정 필요시:**
[tracker_config.py](tracker_config.py) 파일의 `DOT_FADE_ALPHA` 값을 변경하세요!

---

**퀄리티가 좋아졌다니 기쁩니다! 이제 잔상 문제도 해결되었습니다!** 🎉
